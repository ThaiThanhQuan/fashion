export interface ICreateAddress {
    name: string
    phone: string
    address: string
    isDefault: boolean
}

export interface IAddress {
    id: string
    userId: string
    name: string
    phone: string
    address: string
    isDefault: boolean
}